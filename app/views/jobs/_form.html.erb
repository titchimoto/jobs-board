<div class="row">
  <div class="col-md-4">
    <p>Suggestions for Job Listings</p>
    <ul>
      <li>Provide an easy way to contact you</li>
      <li>Where possible, provide accurate salary expectations</li>
      <li>Be transparent with the type of candidate you are looking for</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @job do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: 'Enter Job Listing Title' %>
      </div>

      <div class="form-group">
        <%= f.label :location %>
        <%= f.text_field :location, class: 'form-control', placeholder: "i.e 'Portland, OR'" %>
      </div>

      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: 'Enter job listing information. Provide as many details as possible.' %>
      </div>

      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
