<h1>Edit Profile</h1>
<% if @user.errors.any? %>
<div class="alert alert-danger">
  <h4> <%= pluralize(@user.errors.count, "error") %>.</h4>
  <ul>
    <% @user.errors.full_messages.each do |msg| %>
    <li><%= msg %>
      <% end %>
  </ul>
</div>
<% end %>
<div class="row">
  <div class="col-md-4">
    <p>Suggestions to help maximize your profile potential.</p>
    <ul>
      <li>Provide an easy way to contact you</li>
      <li>Be creative! Don't put the same, boring things everyone else does.</li>
      <li>Let people know why you would be a great fit for them.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @user do |f| %>

      <div class="form-group">
        <%= f.label :bio %>
        <%= f.text_area :bio, rows: 8, class: 'form-control', placeholder: 'Tell prospective employers a little something about yourself...' %>
      </div>

      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>
